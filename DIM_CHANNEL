create or replace TABLE IMT577_DB_LAW_NEELY.PUBLIC.DIM_CHANNEL (
	DIMCHANNELID NUMBER(38,0) NOT NULL autoincrement start 1 increment 1 noorder,
	CHANNELID NUMBER(38,0),
	CHANNELCATEGORYID NUMBER(38,0),
	CHANNELNAME VARCHAR(255),
	CHANNELCATEGORY VARCHAR(255),
	constraint PK_DIMCHANNELID primary key (DIMCHANNELID)
);
